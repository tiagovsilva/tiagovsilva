# Azure Files

| Script | Purpose |
|---|---|
| [`set-storage-sync-network-limit.ps1`](set-storage-sync-network-limit.ps1) | Configures a bandwidth schedule for the local Azure File Sync agent. |

This script remains on Windows PowerShell 5.1 because it loads cmdlets installed with the Windows agent.

