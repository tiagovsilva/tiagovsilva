# Windows Server

| Script | Purpose |
|---|---|
| [`export-server-health.ps1`](export-server-health.ps1) | Exports a local server health snapshot as JSON. |

The generated report contains infrastructure data and must remain outside Git.

