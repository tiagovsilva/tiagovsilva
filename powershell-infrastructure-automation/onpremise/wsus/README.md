# WSUS

| Script | Purpose |
|---|---|
| [`invoke-wsus-cleanup.ps1`](invoke-wsus-cleanup.ps1) | Runs cleanup against an upstream server and optional replicas. |
| [`test-wsus-connection.ps1`](test-wsus-connection.ps1) | Tests TCP and HTTP connectivity to WSUS. |

Run cleanup during an approved maintenance window.

